# COMP-0062: Provider Comparison — Claim extraction EX-Q8-gpt-5-6-terra

## Metadata

- comparison_id: `COMP-0062`
- title: `Claim extraction EX-Q8-gpt-5-6-terra`
- invocation_produced_by: `[{"authoritative": false, "evidence": "docs/invocations/INV-5efa47963fb44b53.json", "predicate": "produced_by", "relation_source": "future_edge_seed", "scope": "invocation_provenance_slice_a", "source_id": "COMP-0062", "target_id": "INV-5efa47963fb44b53"}, {"authoritative": false, "evidence": "docs/invocations/INV-828db1aa0199f6d6.json", "predicate": "produced_by", "relation_source": "future_edge_seed", "scope": "invocation_provenance_slice_a", "source_id": "COMP-0062", "target_id": "INV-828db1aa0199f6d6"}]`
- created_at: `2026-07-28T19:52:39.931230+00:00`
- command: `scripts/compare_providers.py --title Claim extraction EX-Q8-gpt-5-6-terra You are extracting claims from a review answer so they can be recorded as separate, addressable records. You are not evaluating the answer and not deciding whether any claim is correct.

The question that was asked, and the answer given to it, are below. Extract every distinct claim the answer makes.

Rules:

1. One assertion per claim. If a sentence makes two assertions, split it.
2. Extract only what the answer says. Do not add, infer, complete, or improve.
3. PRESERVE HEDGES EXACTLY. If the answer says "may", "appears to", "I cannot determine", "only in the narrow sense that", or "less certain", the claim text must carry that same force. Never state a hedged claim flatly. A claim recorded more confidently than it was made is the most damaging error you can make here.
4. Give each claim a type, one of:
   - "finding": an assertion about the reviewed material
   - "recommendation": something the answer says should be done or changed
   - "evidence_gap": material the answer says it needed and did not have
   - "limitation": a stated bound on the answer's own reliability or reach
5. For each claim give "about": the specific thing it concerns, as precisely as the answer allows - for example "ABS-0004 v7 Section 3 visibility constraint", "PLAN-20260723-0002 scope[12]", "refusal reason 9". Use "unspecified" if the answer does not say.
6. For each claim give "quote": a short verbatim passage from the answer, under 30 words, that states it. The quote must appear in the answer exactly.

Output STRICT JSON and nothing else. No preamble, no commentary, no markdown fences. Exactly this shape:

{"claims":[{"text":"...","type":"finding","about":"...","quote":"..."}]}

If the answer makes no extractable claim, output {"claims":[]}.

=== QUESTION THAT WAS ASKED (Q8) ===
Which decisions in this slice constrain later evidence admission and lineage work, and which of them would be expensive to reverse once records exist?

Distinguish decisions that only require changing validation code from decisions that would require migrating existing records, and say which category each falls in.

=== ANSWER TO EXTRACT FROM ===
Neither proposed instrument has governance force. The following are therefore prospective design commitments, not admitted requirements.

| Decision in this slice | Constraint on later evidence admission / lineage work | Reversal category once records exist |
|---|---|---|
| `RoleDefinition` uses the three axes and carries constraint fields, especially `required_verified_properties`, `required_independence`, `requires_external_verification`, and `may_assign_epistemic_status` | `EvidenceAdmissionDecision` must enumerate verified properties against the receiving role’s `required_verified_properties`; it cannot treat an uncovered property as established. Role definitions therefore determine what later admission must prove and which outputs can carry epistemic status. | **Existing-record migration and reassessment.** Changing axes, values, or the meaning of a required property would require updating role records and re-evaluating admission decisions that relied on those roles. Adding a validator for an unchanged role schema is **validation-code only**. |
| The distinction between task, epistemic, action, and accountable authority | Admission cannot infer acceptance authority from artifact production, and later lineage work must retain the distinction between an executor, an approver, and an accountable principal. | **Existing-record migration** if identities or fields have been collapsed in stored records. A new validator enforcing an already-recorded separation is **validation-code only**, though historical records may need reclassification if their prior status depended on the collapse. |
| `DecisionRecord` inherited fields, including distinct `recommended_status` and `effective_status`, issuer invocation, accountable approver, evidence, policy authority, activation state, and supersession | Later admission requires a record that distinguishes a recommendation from accountable final status and can identify the evidence, authority, and supersession state on which admission relied. | **Existing-record migration.** Adding inherited fields or splitting a previously conflated status changes persisted decision meaning and requires backfill or explicit unknown values for old records. |
| `EvidenceAdmissionDecision` has admitted artifact or claim, epistemic status, lineage and independence findings, provenance limitations, and verified-property enumeration | This is the direct future admission schema. It fixes the evidence needed to explain why an artifact or claim entered governed evidence and prevents unsupported property assertions. | **Existing-record migration and re-adjudication.** Records admitted without these fields cannot simply be treated as if they contained lineage, independence, or verification findings. Validators alone can enforce the rule only for new decisions. |
| The open choice whether existing warrant/admission records are extended or a new record family is created, and whether kinds are subtypes or one family | This unresolved choice controls record identity, references, query patterns, supersession, and how authorization and admission records relate. | **Expensive existing-record migration if records are created before resolution.** A later family split, merge, or subtype conversion would require transforming records and their references. This is not merely a validator change. |
| Execution-scoped `RoleQualification`: endpoint, `ExecutionProfile` constraints, session-state constraints, and policy version; no implicit transfer | Admission and lineage work can rely only on qualifications tied to the executor’s actual execution context. A later admission cannot safely treat an executor as qualified merely because it was qualified elsewhere. | **Existing-record migration/revalidation** if the scope fields or transfer rule change, because old qualification records and authorizations relying on them must be rechecked. Rejecting new out-of-scope qualifications under the unchanged representation is **validation-code only**. |
| `AccountablePrincipal` with `authority_scope` and `delegation_reference`; authorization chains terminate at a recorded standing-authority claim within scope | Later admission has a recorded authority path and can distinguish an in-scope authority claim from an unterminated or out-of-scope authorization. The plan also makes root authority extra-systemic rather than internally verified. | **Existing-record migration and re-evaluation** if scope or delegation semantics change, because authorization-chain conclusions depend on them. Adding scope validation for records already containing the needed fields is **validation-code only** for future use, but historical governed classifications would need recomputation if they are retained as conclusions. |
| `self_issued` marking and the rule that self-issued authorization cannot count as independent where independence is required | This directly constrains later independence findings in evidence admission. A self-issued authorization may exist, but cannot supply the independent authorization that a receiving role requires. | **Mixed.** Recording `self_issued` and changing the meaning of independence for existing authorizations requires **migration or derived-result backfill**. Improving detection while retaining the same stored identifiers is **validation-code only** prospectively; prior independence findings need re-evaluation. |
| Exact identifier equality is the plan’s only party-sameness test, with broader sameness explicitly not detected | Later lineage and independence work will miss collisions represented by different identifiers unless identity-equivalence work is added. This bounds the evidentiary meaning of the plan’s self-adjudication and self-issued results. | **Primarily validation/identity-resolution code**, if original identifiers remain intact. But any historical “independent” result or permitted one-hop result based on exact equality would require **re-evaluation and likely migration of derived findings** if broader equivalence is later adopted. |
| One-hop self-adjudication check, explicitly marked `check_depth` as direct-only | It supplies only the direct producer/executor collision check. It does not establish multi-hop lineage independence, which the plan defers to Slice D. Consumers of later admission records must not interpret a permitted result as a general independence finding. | **Mixed.** Extending traversal is initially **validation-code only** if complete lineage edges already exist. However, old direct-only outcomes and any downstream admission findings relying on them require **re-evaluation or migration of derived records**. Missing ancestry links cannot be repaired by code alone. |
| Composite-executor disclosure through `spawned` invocation links | Future lineage must represent material model, tool, and human subexecutions rather than allowing authority or influence to disappear inside a tool or composite executor. This is foundational for later multi-hop lineage analysis. | **Expensive existing-record migration, and potentially irrecoverable historical gaps.** New validation can reject undisclosed composition going forward, but it cannot reconstruct unrecorded subordinate executions in existing lineage. |
| Tool-configuration disclosure through `EffectiveInputManifest`, including content-addressed configuration, environment, and local dependencies | Future evidence admission and deterministic-standing assessments depend on knowing the effective inputs to a governed-evidence-producing tool. | **Expensive migration/backfill; potentially impossible.** A validator can enforce manifests for new records, but historical configuration, environment, or dependency state may not be recoverable. |
| External-interaction disclosure and `OutboundInteractionLog` | Later lineage and evidence admission must account for material external interactions and ambient consumers, not merely returned output bytes. This affects whether an invocation can be considered deterministic and whether undeclared `external_actuator` authority existed. | **Expensive migration/backfill; potentially impossible.** Enforcement can be added in validation or capture code for future invocations, but absent historical interaction logs generally cannot be recreated reliably. |
| Invocation executor kinds remain distinct, while the `ExecutorIdentity` superclass question is open | Later lineage constraints may need polymorphic rules across models, tools, and humans. The open superclass question affects how uniformly constraints such as identity and executor checks can be expressed. | **Potentially expensive schema migration** if records are first stored under one identity model and later require a superclass or subtype-specific equivalence semantics. Implementing polymorphic validation without changing stored identity shapes could be **validation-code only**, but that depends on identity fields and relations not supplied here. |
| Fixed Slice C predicates and the plan’s requirement that relations use `GraphRelation` and registered predicate lists | Admission and lineage records will need stable relation names and shapes for such matters as authorization, qualification, approval, production, execution, and ancestry. | **Existing-record migration** if persisted predicates or relation shapes are renamed, split, merged, or reinterpreted. Registering a new predicate without changing existing ones is generally **schema/validation-code only**. |
| Governed versus experimental classification: governed only when a valid authorization covers the invocation; pre-slice invocations remain experimental | Later admission work inherits a provenance boundary: an invocation without valid authorization is not retroactively made governed by this slice. This limits what future admission can claim about earlier records. | **Reclassification and possibly migration of stored labels/derived decisions** if the boundary changes. Altering the classifier alone is **validation-code only** only if historical classifications are retained as time-versioned results rather than overwritten. |
| Consequence classes, role ceilings, modifiers that only raise, and controlled downward adjustment | These determine which roles and authorization conditions are applicable to later evidence-producing or admitting actions. They are described as load-bearing for Slice D. | **Existing-record migration and re-evaluation** if vocabulary ordering, defaults, modifier semantics, or exception requirements change, because stored authorization and admission conclusions may change. Adding checks for unchanged rules is **validation-code only** prospectively. |
| Authorization is classification only and does not refuse execution | This means later evidence admission cannot equate execution occurrence with a successfully governed authorization check. It also preserves a population of executed but experimental invocations that Slice D may need to handle explicitly. | **Primarily validation/workflow code** to change prospectively. If records or policies have relied on the distinction between executed, governed, and experimental, changing it would require **reclassification of existing records**, but not necessarily rewriting invocation capture. |

The decisions most expensive to reverse are the ones that determine what was captured at execution time: material subordinate invocations, `spawned` lineage links, effective input manifests, and outbound interaction logs. A later validator can reject future omissions, but cannot reliably reconstruct omitted historical provenance. The next most expensive are record-family, role-vocabulary, identity, qualification-scope, and authority-chain choices, because they affect the meaning and references of authorization and future admission records.

The plan’s one-hop check is comparatively cheap to extend in code only if the required lineage has already been captured. It is not cheap to upgrade the evidentiary meaning of existing direct-only results: those results would need re-evaluation, and missing lineage would remain missing.

Several needed materials are absent if a more precise migration assessment is required:

- Section 8, to determine the full consequence-class vocabulary, modifier set, and compatibility/migration consequences.
- Section 11, to determine the required shape, retention, and linkage rules for `OutboundInteractionLog`.
- The definitions of `GraphRelation`, the Slice A/B/C predicate lists, and the `produced_by`, `executed_by`, and `spawned` record shapes, to assess whether future multi-hop traversal can be added without changing stored records.
- The applicable definitions of C3, P1, P4, and P7, to determine the precise scope of independence, authority, disclosure, and root-authority claims.
- Any retention, versioning, supersession, and migration rules for existing records, to determine whether changed validation should preserve historical outcomes under their original policy versions or rewrite/reclassify them.
- The existing warrant/admission record schemas, needed to resolve the expressly open choice between extending them and creating a new `DecisionRecord` family.

--- END OF ANSWER ---

Output the JSON now.`
- providers: `OpenAI, Claude`

### Models

- OpenAI: `gpt-5.6-terra`
- Claude: `claude-sonnet-5`

## Prompt

You are extracting claims from a review answer so they can be recorded as separate, addressable records. You are not evaluating the answer and not deciding whether any claim is correct.

The question that was asked, and the answer given to it, are below. Extract every distinct claim the answer makes.

Rules:

1. One assertion per claim. If a sentence makes two assertions, split it.
2. Extract only what the answer says. Do not add, infer, complete, or improve.
3. PRESERVE HEDGES EXACTLY. If the answer says "may", "appears to", "I cannot determine", "only in the narrow sense that", or "less certain", the claim text must carry that same force. Never state a hedged claim flatly. A claim recorded more confidently than it was made is the most damaging error you can make here.
4. Give each claim a type, one of:
   - "finding": an assertion about the reviewed material
   - "recommendation": something the answer says should be done or changed
   - "evidence_gap": material the answer says it needed and did not have
   - "limitation": a stated bound on the answer's own reliability or reach
5. For each claim give "about": the specific thing it concerns, as precisely as the answer allows - for example "ABS-0004 v7 Section 3 visibility constraint", "PLAN-20260723-0002 scope[12]", "refusal reason 9". Use "unspecified" if the answer does not say.
6. For each claim give "quote": a short verbatim passage from the answer, under 30 words, that states it. The quote must appear in the answer exactly.

Output STRICT JSON and nothing else. No preamble, no commentary, no markdown fences. Exactly this shape:

{"claims":[{"text":"...","type":"finding","about":"...","quote":"..."}]}

If the answer makes no extractable claim, output {"claims":[]}.

=== QUESTION THAT WAS ASKED (Q8) ===
Which decisions in this slice constrain later evidence admission and lineage work, and which of them would be expensive to reverse once records exist?

Distinguish decisions that only require changing validation code from decisions that would require migrating existing records, and say which category each falls in.

=== ANSWER TO EXTRACT FROM ===
Neither proposed instrument has governance force. The following are therefore prospective design commitments, not admitted requirements.

| Decision in this slice | Constraint on later evidence admission / lineage work | Reversal category once records exist |
|---|---|---|
| `RoleDefinition` uses the three axes and carries constraint fields, especially `required_verified_properties`, `required_independence`, `requires_external_verification`, and `may_assign_epistemic_status` | `EvidenceAdmissionDecision` must enumerate verified properties against the receiving role’s `required_verified_properties`; it cannot treat an uncovered property as established. Role definitions therefore determine what later admission must prove and which outputs can carry epistemic status. | **Existing-record migration and reassessment.** Changing axes, values, or the meaning of a required property would require updating role records and re-evaluating admission decisions that relied on those roles. Adding a validator for an unchanged role schema is **validation-code only**. |
| The distinction between task, epistemic, action, and accountable authority | Admission cannot infer acceptance authority from artifact production, and later lineage work must retain the distinction between an executor, an approver, and an accountable principal. | **Existing-record migration** if identities or fields have been collapsed in stored records. A new validator enforcing an already-recorded separation is **validation-code only**, though historical records may need reclassification if their prior status depended on the collapse. |
| `DecisionRecord` inherited fields, including distinct `recommended_status` and `effective_status`, issuer invocation, accountable approver, evidence, policy authority, activation state, and supersession | Later admission requires a record that distinguishes a recommendation from accountable final status and can identify the evidence, authority, and supersession state on which admission relied. | **Existing-record migration.** Adding inherited fields or splitting a previously conflated status changes persisted decision meaning and requires backfill or explicit unknown values for old records. |
| `EvidenceAdmissionDecision` has admitted artifact or claim, epistemic status, lineage and independence findings, provenance limitations, and verified-property enumeration | This is the direct future admission schema. It fixes the evidence needed to explain why an artifact or claim entered governed evidence and prevents unsupported property assertions. | **Existing-record migration and re-adjudication.** Records admitted without these fields cannot simply be treated as if they contained lineage, independence, or verification findings. Validators alone can enforce the rule only for new decisions. |
| The open choice whether existing warrant/admission records are extended or a new record family is created, and whether kinds are subtypes or one family | This unresolved choice controls record identity, references, query patterns, supersession, and how authorization and admission records relate. | **Expensive existing-record migration if records are created before resolution.** A later family split, merge, or subtype conversion would require transforming records and their references. This is not merely a validator change. |
| Execution-scoped `RoleQualification`: endpoint, `ExecutionProfile` constraints, session-state constraints, and policy version; no implicit transfer | Admission and lineage work can rely only on qualifications tied to the executor’s actual execution context. A later admission cannot safely treat an executor as qualified merely because it was qualified elsewhere. | **Existing-record migration/revalidation** if the scope fields or transfer rule change, because old qualification records and authorizations relying on them must be rechecked. Rejecting new out-of-scope qualifications under the unchanged representation is **validation-code only**. |
| `AccountablePrincipal` with `authority_scope` and `delegation_reference`; authorization chains terminate at a recorded standing-authority claim within scope | Later admission has a recorded authority path and can distinguish an in-scope authority claim from an unterminated or out-of-scope authorization. The plan also makes root authority extra-systemic rather than internally verified. | **Existing-record migration and re-evaluation** if scope or delegation semantics change, because authorization-chain conclusions depend on them. Adding scope validation for records already containing the needed fields is **validation-code only** for future use, but historical governed classifications would need recomputation if they are retained as conclusions. |
| `self_issued` marking and the rule that self-issued authorization cannot count as independent where independence is required | This directly constrains later independence findings in evidence admission. A self-issued authorization may exist, but cannot supply the independent authorization that a receiving role requires. | **Mixed.** Recording `self_issued` and changing the meaning of independence for existing authorizations requires **migration or derived-result backfill**. Improving detection while retaining the same stored identifiers is **validation-code only** prospectively; prior independence findings need re-evaluation. |
| Exact identifier equality is the plan’s only party-sameness test, with broader sameness explicitly not detected | Later lineage and independence work will miss collisions represented by different identifiers unless identity-equivalence work is added. This bounds the evidentiary meaning of the plan’s self-adjudication and self-issued results. | **Primarily validation/identity-resolution code**, if original identifiers remain intact. But any historical “independent” result or permitted one-hop result based on exact equality would require **re-evaluation and likely migration of derived findings** if broader equivalence is later adopted. |
| One-hop self-adjudication check, explicitly marked `check_depth` as direct-only | It supplies only the direct producer/executor collision check. It does not establish multi-hop lineage independence, which the plan defers to Slice D. Consumers of later admission records must not interpret a permitted result as a general independence finding. | **Mixed.** Extending traversal is initially **validation-code only** if complete lineage edges already exist. However, old direct-only outcomes and any downstream admission findings relying on them require **re-evaluation or migration of derived records**. Missing ancestry links cannot be repaired by code alone. |
| Composite-executor disclosure through `spawned` invocation links | Future lineage must represent material model, tool, and human subexecutions rather than allowing authority or influence to disappear inside a tool or composite executor. This is foundational for later multi-hop lineage analysis. | **Expensive existing-record migration, and potentially irrecoverable historical gaps.** New validation can reject undisclosed composition going forward, but it cannot reconstruct unrecorded subordinate executions in existing lineage. |
| Tool-configuration disclosure through `EffectiveInputManifest`, including content-addressed configuration, environment, and local dependencies | Future evidence admission and deterministic-standing assessments depend on knowing the effective inputs to a governed-evidence-producing tool. | **Expensive migration/backfill; potentially impossible.** A validator can enforce manifests for new records, but historical configuration, environment, or dependency state may not be recoverable. |
| External-interaction disclosure and `OutboundInteractionLog` | Later lineage and evidence admission must account for material external interactions and ambient consumers, not merely returned output bytes. This affects whether an invocation can be considered deterministic and whether undeclared `external_actuator` authority existed. | **Expensive migration/backfill; potentially impossible.** Enforcement can be added in validation or capture code for future invocations, but absent historical interaction logs generally cannot be recreated reliably. |
| Invocation executor kinds remain distinct, while the `ExecutorIdentity` superclass question is open | Later lineage constraints may need polymorphic rules across models, tools, and humans. The open superclass question affects how uniformly constraints such as identity and executor checks can be expressed. | **Potentially expensive schema migration** if records are first stored under one identity model and later require a superclass or subtype-specific equivalence semantics. Implementing polymorphic validation without changing stored identity shapes could be **validation-code only**, but that depends on identity fields and relations not supplied here. |
| Fixed Slice C predicates and the plan’s requirement that relations use `GraphRelation` and registered predicate lists | Admission and lineage records will need stable relation names and shapes for such matters as authorization, qualification, approval, production, execution, and ancestry. | **Existing-record migration** if persisted predicates or relation shapes are renamed, split, merged, or reinterpreted. Registering a new predicate without changing existing ones is generally **schema/validation-code only**. |
| Governed versus experimental classification: governed only when a valid authorization covers the invocation; pre-slice invocations remain experimental | Later admission work inherits a provenance boundary: an invocation without valid authorization is not retroactively made governed by this slice. This limits what future admission can claim about earlier records. | **Reclassification and possibly migration of stored labels/derived decisions** if the boundary changes. Altering the classifier alone is **validation-code only** only if historical classifications are retained as time-versioned results rather than overwritten. |
| Consequence classes, role ceilings, modifiers that only raise, and controlled downward adjustment | These determine which roles and authorization conditions are applicable to later evidence-producing or admitting actions. They are described as load-bearing for Slice D. | **Existing-record migration and re-evaluation** if vocabulary ordering, defaults, modifier semantics, or exception requirements change, because stored authorization and admission conclusions may change. Adding checks for unchanged rules is **validation-code only** prospectively. |
| Authorization is classification only and does not refuse execution | This means later evidence admission cannot equate execution occurrence with a successfully governed authorization check. It also preserves a population of executed but experimental invocations that Slice D may need to handle explicitly. | **Primarily validation/workflow code** to change prospectively. If records or policies have relied on the distinction between executed, governed, and experimental, changing it would require **reclassification of existing records**, but not necessarily rewriting invocation capture. |

The decisions most expensive to reverse are the ones that determine what was captured at execution time: material subordinate invocations, `spawned` lineage links, effective input manifests, and outbound interaction logs. A later validator can reject future omissions, but cannot reliably reconstruct omitted historical provenance. The next most expensive are record-family, role-vocabulary, identity, qualification-scope, and authority-chain choices, because they affect the meaning and references of authorization and future admission records.

The plan’s one-hop check is comparatively cheap to extend in code only if the required lineage has already been captured. It is not cheap to upgrade the evidentiary meaning of existing direct-only results: those results would need re-evaluation, and missing lineage would remain missing.

Several needed materials are absent if a more precise migration assessment is required:

- Section 8, to determine the full consequence-class vocabulary, modifier set, and compatibility/migration consequences.
- Section 11, to determine the required shape, retention, and linkage rules for `OutboundInteractionLog`.
- The definitions of `GraphRelation`, the Slice A/B/C predicate lists, and the `produced_by`, `executed_by`, and `spawned` record shapes, to assess whether future multi-hop traversal can be added without changing stored records.
- The applicable definitions of C3, P1, P4, and P7, to determine the precise scope of independence, authority, disclosure, and root-authority claims.
- Any retention, versioning, supersession, and migration rules for existing records, to determine whether changed validation should preserve historical outcomes under their original policy versions or rewrite/reclassify them.
- The existing warrant/admission record schemas, needed to resolve the expressly open choice between extending them and creating a new `DecisionRecord` family.

--- END OF ANSWER ---

Output the JSON now.

## OpenAI Response

- model: `gpt-5.6-terra`

```
{"claims":[{"text":"Neither proposed instrument has governance force.","type":"finding","about":"proposed instruments","quote":"Neither proposed instrument has governance force."},{"text":"The listed decisions are prospective design commitments, not admitted requirements.","type":"finding","about":"listed decisions","quote":"prospective design commitments, not admitted requirements"},{"text":"EvidenceAdmissionDecision must enumerate verified properties against the receiving role’s required_verified_properties.","type":"finding","about":"EvidenceAdmissionDecision verified-property enumeration","quote":"EvidenceAdmissionDecision must enumerate verified properties against the receiving role’s `required_verified_properties`"},{"text":"EvidenceAdmissionDecision cannot treat an uncovered property as established.","type":"finding","about":"EvidenceAdmissionDecision uncovered properties","quote":"it cannot treat an uncovered property as established"},{"text":"Role definitions determine what later admission must prove.","type":"finding","about":"RoleDefinition","quote":"Role definitions therefore determine what later admission must prove"},{"text":"Role definitions determine which outputs can carry epistemic status.","type":"finding","about":"RoleDefinition","quote":"which outputs can carry epistemic status"},{"text":"Changing RoleDefinition axes, values, or the meaning of a required property would require updating role records.","type":"finding","about":"RoleDefinition axes, values, and required-property meaning","quote":"would require updating role records"},{"text":"Changing RoleDefinition axes, values, or the meaning of a required property would require re-evaluating admission decisions that relied on those roles.","type":"finding","about":"admission decisions relying on RoleDefinition","quote":"re-evaluating admission decisions that relied on those roles"},{"text":"Adding a validator for an unchanged role schema is validation-code only.","type":"finding","about":"unchanged RoleDefinition schema","quote":"Adding a validator for an unchanged role schema is **validation-code only**."},{"text":"Admission cannot infer acceptance authority from artifact production.","type":"finding","about":"admission authority inference","quote":"Admission cannot infer acceptance authority from artifact production"},{"text":"Later lineage work must retain the distinction between an executor, an approver, and an accountable principal.","type":"finding","about":"later lineage work","quote":"later lineage work must retain the distinction between an executor, an approver, and an accountable principal"},{"text":"Existing-record migration is required if identities or fields have been collapsed in stored records.","type":"finding","about":"stored records with collapsed identities or fields","quote":"**Existing-record migration** if identities or fields have been collapsed in stored records."},{"text":"A new validator enforcing an already-recorded separation is validation-code only.","type":"finding","about":"already-recorded authority separation","quote":"A new validator enforcing an already-recorded separation is **validation-code only**"},{"text":"Historical records may need reclassification if their prior status depended on the collapse.","type":"finding","about":"historical records whose status depended on collapsed identities or fields","quote":"historical records may need reclassification if their prior status depended on the collapse"},{"text":"Later admission requires a record that distinguishes a recommendation from accountable final status.","type":"finding","about":"DecisionRecord status fields","quote":"Later admission requires a record that distinguishes a recommendation from accountable final status"},{"text":"Later admission requires a record that can identify the evidence, authority, and supersession state on which admission relied.","type":"finding","about":"DecisionRecord evidence, authority, and supersession","quote":"can identify the evidence, authority, and supersession state on which admission relied"},{"text":"Adding inherited fields or splitting a previously conflated status changes persisted decision meaning.","type":"finding","about":"DecisionRecord inherited fields and statuses","quote":"changes persisted decision meaning"},{"text":"Adding inherited fields or splitting a previously conflated status requires backfill or explicit unknown values for old records.","type":"finding","about":"old DecisionRecord records","quote":"requires backfill or explicit unknown values for old records"},{"text":"EvidenceAdmissionDecision is the direct future admission schema.","type":"finding","about":"EvidenceAdmissionDecision","quote":"This is the direct future admission schema."},{"text":"EvidenceAdmissionDecision fixes the evidence needed to explain why an artifact or claim entered governed evidence.","type":"finding","about":"EvidenceAdmissionDecision","quote":"It fixes the evidence needed to explain why an artifact or claim entered governed evidence"},{"text":"EvidenceAdmissionDecision prevents unsupported property assertions.","type":"finding","about":"EvidenceAdmissionDecision","quote":"prevents unsupported property assertions"},{"text":"Records admitted without lineage, independence, or verification fields cannot simply be treated as if they contained those findings.","type":"finding","about":"admission records without required fields","quote":"cannot simply be treated as if they contained lineage, independence, or verification findings"},{"text":"Validators alone can enforce the EvidenceAdmissionDecision rule only for new decisions.","type":"finding","about":"EvidenceAdmissionDecision validators","quote":"Validators alone can enforce the rule only for new decisions."},{"text":"The unresolved choice between extending existing warrant/admission records or creating a new record family controls record identity.","type":"finding","about":"warrant/admission record-family choice","quote":"This unresolved choice controls record identity"},{"text":"The unresolved record-family choice controls references, query patterns, supersession, and how authorization and admission records relate.","type":"finding","about":"warrant/admission record-family choice","quote":"references, query patterns, supersession, and how authorization and admission records relate"},{"text":"An expensive existing-record migration is required if records are created before the record-family choice is resolved.","type":"finding","about":"unresolved warrant/admission record-family choice","quote":"**Expensive existing-record migration if records are created before resolution.**"},{"text":"A later family split, merge, or subtype conversion would require transforming records and their references.","type":"finding","about":"record-family split, merge, or subtype conversion","quote":"would require transforming records and their references"},{"text":"A later family split, merge, or subtype conversion is not merely a validator change.","type":"finding","about":"record-family split, merge, or subtype conversion","quote":"This is not merely a validator change."},{"text":"Admission and lineage work can rely only on qualifications tied to the executor’s actual execution context.","type":"finding","about":"execution-scoped RoleQualification","quote":"can rely only on qualifications tied to the executor’s actual execution context"},{"text":"A later admission cannot safely treat an executor as qualified merely because it was qualified elsewhere.","type":"finding","about":"execution-scoped RoleQualification","quote":"cannot safely treat an executor as qualified merely because it was qualified elsewhere"},{"text":"Scope-field or transfer-rule changes require existing-record migration or revalidation because old qualification records and authorizations relying on them must be rechecked.","type":"finding","about":"RoleQualification scope fields and transfer rule","quote":"old qualification records and authorizations relying on them must be rechecked"},{"text":"Rejecting new out-of-scope qualifications under the unchanged representation is validation-code only.","type":"finding","about":"unchanged RoleQualification representation","quote":"Rejecting new out-of-scope qualifications under the unchanged representation is **validation-code only**."},{"text":"Later admission has a recorded authority path.","type":"finding","about":"AccountablePrincipal authority chain","quote":"Later admission has a recorded authority path"},{"text":"Later admission can distinguish an in-scope authority claim from an unterminated or out-of-scope authorization.","type":"finding","about":"AccountablePrincipal authority chain","quote":"can distinguish an in-scope authority claim from an unterminated or out-of-scope authorization"},{"text":"The plan makes root authority extra-systemic rather than internally verified.","type":"finding","about":"root authority","quote":"The plan also makes root authority extra-systemic rather than internally verified."},{"text":"Changing scope or delegation semantics requires existing-record migration and re-evaluation because authorization-chain conclusions depend on them.","type":"finding","about":"AccountablePrincipal authority_scope and delegation_reference semantics","quote":"authorization-chain conclusions depend on them"},{"text":"Adding scope validation for records already containing the needed fields is validation-code only for future use.","type":"finding","about":"records containing needed scope fields","quote":"Adding scope validation for records already containing the needed fields is **validation-code only** for future use"},{"text":"Historical governed classifications would need recomputation if they are retained as conclusions.","type":"finding","about":"historical governed classifications","quote":"historical governed classifications would need recomputation if they are retained as conclusions"},{"text":"Self-issued authorization cannot count as independent where independence is required.","type":"finding","about":"self-issued authorization","quote":"self-issued authorization cannot count as independent where independence is required"},{"text":"A self-issued authorization may exist.","type":"finding","about":"self-issued authorization","quote":"A self-issued authorization may exist"},{"text":"A self-issued authorization cannot supply the independent authorization that a receiving role requires.","type":"finding","about":"self-issued authorization","quote":"cannot supply the independent authorization that a receiving role requires"},{"text":"Recording self_issued and changing the meaning of independence for existing authorizations requires migration or derived-result backfill.","type":"finding","about":"existing authorizations and self_issued","quote":"requires **migration or derived-result backfill**"},{"text":"Improving detection while retaining the same stored identifiers is validation-code only prospectively.","type":"finding","about":"self-issued detection with unchanged identifiers","quote":"Improving detection while retaining the same stored identifiers is **validation-code only** prospectively"},{"text":"Prior independence findings need re-evaluation.","type":"finding","about":"prior independence findings","quote":"prior independence findings need re-evaluation"},{"text":"Exact identifier equality is the plan’s only party-sameness test.","type":"finding","about":"party-sameness test","quote":"Exact identifier equality is the plan’s only party-sameness test"},{"text":"Broader sameness is explicitly not detected.","type":"finding","about":"broader party sameness","quote":"with broader sameness explicitly not detected"},{"text":"Later lineage and independence work will miss collisions represented by different identifiers unless identity-equivalence work is added.","type":"finding","about":"lineage and independence work","quote":"will miss collisions represented by different identifiers unless identity-equivalence work is added"},{"text":"Exact identifier equality bounds the evidentiary meaning of the plan’s self-adjudication and self-issued results.","type":"finding","about":"self-adjudication and self-issued results","quote":"This bounds the evidentiary meaning of the plan’s self-adjudication and self-issued results."},{"text":"The reversal is primarily validation/identity-resolution code if original identifiers remain intact.","type":"finding","about":"exact-equality party-sameness test","quote":"**Primarily validation/identity-resolution code**, if original identifiers remain intact."},{"text":"Any historical independent result or permitted one-hop result based on exact equality would require re-evaluation and likely migration of derived findings if broader equivalence is later adopted.","type":"finding","about":"historical exact-equality independence and one-hop results","quote":"would require **re-evaluation and likely migration of derived findings** if broader equivalence is later adopted"},{"text":"The one-hop self-adjudication check supplies only the direct producer/executor collision check.","type":"finding","about":"one-hop self-adjudication check","quote":"It supplies only the direct producer/executor collision check."},{"text":"The one-hop self-adjudication check does not establish multi-hop lineage independence.","type":"finding","about":"one-hop self-adjudication check","quote":"It does not establish multi-hop lineage independence"},{"text":"The plan defers multi-hop lineage independence to Slice D.","type":"finding","about":"multi-hop lineage independence","quote":"which the plan defers to Slice D"},{"text":"Consumers of later admission records must not interpret a permitted one-hop result as a general independence finding.","type":"finding","about":"permitted one-hop result","quote":"must not interpret a permitted result as a general independence finding"},{"text":"Extending traversal is initially validation-code only if complete lineage edges already exist.","type":"finding","about":"one-hop traversal extension","quote":"Extending traversal is initially **validation-code only** if complete lineage edges already exist."},{"text":"Old direct-only outcomes and downstream admission findings relying on them require re-evaluation or migration of derived records.","type":"finding","about":"old direct-only outcomes","quote":"require **re-evaluation or migration of derived records**"},{"text":"Missing ancestry links cannot be repaired by code alone.","type":"finding","about":"missing ancestry links","quote":"Missing ancestry links cannot be repaired by code alone."},{"text":"Future lineage must represent material model, tool, and human subexecutions.","type":"finding","about":"spawned invocation links","quote":"Future lineage must represent material model, tool, and human subexecutions"},{"text":"Future lineage must not allow authority or influence to disappear inside a tool or composite executor.","type":"finding","about":"spawned invocation links","quote":"rather than allowing authority or influence to disappear inside a tool or composite executor"},{"text":"Composite-executor disclosure is foundational for later multi-hop lineage analysis.","type":"finding","about":"composite-executor disclosure","quote":"This is foundational for later multi-hop lineage analysis."},{"text":"Composite-executor disclosure requires expensive existing-record migration and potentially has irrecoverable historical gaps.","type":"finding","about":"composite-executor disclosure","quote":"**Expensive existing-record migration, and potentially irrecoverable historical gaps.**"},{"text":"New validation can reject undisclosed composition going forward.","type":"finding","about":"undisclosed composite execution","quote":"New validation can reject undisclosed composition going forward"},{"text":"New validation cannot reconstruct unrecorded subordinate executions in existing lineage.","type":"finding","about":"existing lineage","quote":"it cannot reconstruct unrecorded subordinate executions in existing lineage"},{"text":"Future evidence admission and deterministic-standing assessments depend on knowing the effective inputs to a governed-evidence-producing tool.","type":"finding","about":"EffectiveInputManifest","quote":"depend on knowing the effective inputs to a governed-evidence-producing tool"},{"text":"Tool-configuration disclosure requires expensive migration/backfill and is potentially impossible.","type":"finding","about":"EffectiveInputManifest","quote":"**Expensive migration/backfill; potentially impossible.**"},{"text":"A validator can enforce manifests for new records.","type":"finding","about":"EffectiveInputManifest validators","quote":"A validator can enforce manifests for new records"},{"text":"Historical configuration, environment, or dependency state may not be recoverable.","type":"finding","about":"historical tool configuration, environment, and dependencies","quote":"historical configuration, environment, or dependency state may not be recoverable"},{"text":"Later lineage and evidence admission must account for material external interactions and ambient consumers, not merely returned output bytes.","type":"finding","about":"OutboundInteractionLog","quote":"must account for material external interactions and ambient consumers, not merely returned output bytes"},{"text":"External-interaction disclosure affects whether an invocation can be considered deterministic.","type":"finding","about":"external-interaction disclosure","quote":"This affects whether an invocation can be considered deterministic"},{"text":"External-interaction disclosure affects whether undeclared external_actuator authority existed.","type":"finding","about":"external-interaction disclosure","quote":"whether undeclared `external_actuator` authority existed"},{"text":"External-interaction disclosure requires expensive migration/backfill and is potentially impossible.","type":"finding","about":"OutboundInteractionLog","quote":"**Expensive migration/backfill; potentially impossible.**"},{"text":"Enforcement can be added in validation or capture code for future invocations.","type":"finding","about":"OutboundInteractionLog enforcement","quote":"Enforcement can be added in validation or capture code for future invocations"},{"text":"Absent historical interaction logs generally cannot be recreated reliably.","type":"finding","about":"historical OutboundInteractionLog records","quote":"absent historical interaction logs generally cannot be recreated reliably"},{"text":"Later lineage constraints may need polymorphic rules across models, tools, and humans.","type":"finding","about":"invocation executor kinds","quote":"Later lineage constraints may need polymorphic rules across models, tools, and humans."},{"text":"The open ExecutorIdentity superclass question affects how uniformly identity and executor checks can be expressed.","type":"finding","about":"ExecutorIdentity superclass question","quote":"affects how uniformly constraints such as identity and executor checks can be expressed"},{"text":"A schema migration is potentially expensive if records are first stored under one identity model and later require a superclass or subtype-specific equivalence semantics.","type":"finding","about":"ExecutorIdentity schema choice","quote":"**Potentially expensive schema migration** if records are first stored under one identity model"},{"text":"Implementing polymorphic validation without changing stored identity shapes could be validation-code only.","type":"finding","about":"polymorphic validation","quote":"could be **validation-code only**"},{"text":"Whether polymorphic validation can be validation-code only depends on identity fields and relations not supplied here.","type":"limitation","about":"polymorphic validation assessment","quote":"but that depends on identity fields and relations not supplied here"},{"text":"Admission and lineage records will need stable relation names and shapes for authorization, qualification, approval, production, execution, and ancestry.","type":"finding","about":"GraphRelation and registered predicate lists","quote":"will need stable relation names and shapes for such matters as authorization, qualification, approval, production, execution, and ancestry"},{"text":"Persisted predicate or relation-shape changes require existing-record migration if they are renamed, split, merged, or reinterpreted.","type":"finding","about":"persisted GraphRelation predicates and shapes","quote":"**Existing-record migration** if persisted predicates or relation shapes are renamed, split, merged, or reinterpreted."},{"text":"Registering a new predicate without changing existing ones is generally schema/validation-code only.","type":"finding","about":"new GraphRelation predicate","quote":"Registering a new predicate without changing existing ones is generally **schema/validation-code only**."},{"text":"An invocation without valid authorization is not retroactively made governed by this slice.","type":"finding","about":"governed versus experimental classification","quote":"an invocation without valid authorization is not retroactively made governed by this slice"},{"text":"The provenance boundary limits what future admission can claim about earlier records.","type":"finding","about":"pre-slice invocations","quote":"This limits what future admission can claim about earlier records."},{"text":"Changing the governed/experimental boundary requires reclassification and possibly migration of stored labels or derived decisions.","type":"finding","about":"governed versus experimental boundary","quote":"**Reclassification and possibly migration of stored labels/derived decisions** if the boundary changes."},{"text":"Altering the classifier alone is validation-code only only if historical classifications are retained as time-versioned results rather than overwritten.","type":"finding","about":"governed versus experimental classifier","quote":"**validation-code only** only if historical classifications are retained as time-versioned results rather than overwritten"},{"text":"Consequence classes, role ceilings, modifiers that only raise, and controlled downward adjustment determine which roles and authorization conditions are applicable to later evidence-producing or admitting actions.","type":"finding","about":"consequence classes and role ceilings","quote":"These determine which roles and authorization conditions are applicable to later evidence-producing or admitting actions."},{"text":"Consequence classes, role ceilings, modifiers that only raise, and controlled downward adjustment are described as load-bearing for Slice D.","type":"finding","about":"consequence classes and role ceilings","quote":"They are described as load-bearing for Slice D."},{"text":"Changing vocabulary ordering, defaults, modifier semantics, or exception requirements requires existing-record migration and re-evaluation because stored authorization and admission conclusions may change.","type":"finding","about":"consequence classes, role ceilings, and modifiers","quote":"because stored authorization and admission conclusions may change"},{"text":"Adding checks for unchanged consequence and role rules is validation-code only prospectively.","type":"finding","about":"unchanged consequence and role rules","quote":"Adding checks for unchanged rules is **validation-code only** prospectively."},{"text":"Authorization is classification only and does not refuse execution.","type":"finding","about":"authorization","quote":"Authorization is classification only and does not refuse execution"},{"text":"Later evidence admission cannot equate execution occurrence with a successfully governed authorization check.","type":"finding","about":"evidence admission","quote":"cannot equate execution occurrence with a successfully governed authorization check"},{"text":"The distinction preserves a population of executed but experimental invocations that Slice D may need to handle explicitly.","type":"finding","about":"executed but experimental invocations","quote":"preserves a population of executed but experimental invocations that Slice D may need to handle explicitly"},{"text":"Changing authorization classification prospectively is primarily validation/workflow code.","type":"finding","about":"authorization classification","quote":"**Primarily validation/workflow code** to change prospectively."},{"text":"If records or policies have relied on the distinction between executed, governed, and experimental, changing it would require reclassification of existing records.","type":"finding","about":"existing records and policies relying on authorization classification","quote":"changing it would require **reclassification of existing records**"},{"text":"Changing authorization classification would not necessarily require rewriting invocation capture.","type":"finding","about":"authorization classification","quote":"but not necessarily rewriting invocation capture"},{"text":"The decisions most expensive to reverse determine what was captured at execution time.","type":"finding","about":"most expensive decisions to reverse","quote":"The decisions most expensive to reverse are the ones that determine what was captured at execution time"},{"text":"The most expensive decisions include material subordinate invocations, spawned lineage links, effective input manifests, and outbound interaction logs.","type":"finding","about":"most expensive decisions to reverse","quote":"material subordinate invocations, `spawned` lineage links, effective input manifests, and outbound interaction logs"},{"text":"A later validator can reject future omissions.","type":"finding","about":"future provenance omissions","quote":"A later validator can reject future omissions"},{"text":"A later validator cannot reliably reconstruct omitted historical provenance.","type":"finding","about":"omitted historical provenance","quote":"cannot reliably reconstruct omitted historical provenance"},{"text":"Record-family, role-vocabulary, identity, qualification-scope, and authority-chain choices are the next most expensive.","type":"finding","about":"next-most-expensive decisions to reverse","quote":"The next most expensive are record-family, role-vocabulary, identity, qualification-scope, and authority-chain choices"},{"text":"Record-family, role-vocabulary, identity, qualification-scope, and authority-chain choices affect the meaning and references of authorization and future admission records.","type":"finding","about":"record-family, role-vocabulary, identity, qualification-scope, and authority-chain choices","quote":"because they affect the meaning and references of authorization and future admission records"},{"text":"The plan’s one-hop check is comparatively cheap to extend in code only if the required lineage has already been captured.","type":"finding","about":"one-hop check","quote":"comparatively cheap to extend in code only if the required lineage has already been captured"},{"text":"It is not cheap to upgrade the evidentiary meaning of existing direct-only results.","type":"finding","about":"existing direct-only results","quote":"It is not cheap to upgrade the evidentiary meaning of existing direct-only results"},{"text":"Existing direct-only results would need re-evaluation.","type":"finding","about":"existing direct-only results","quote":"those results would need re-evaluation"},{"text":"Missing lineage would remain missing.","type":"finding","about":"missing lineage","quote":"missing lineage would remain missing"},{"text":"Section 8 is absent if a more precise migration assessment is required.","type":"evidence_gap","about":"Section 8","quote":"Section 8, to determine the full consequence-class vocabulary"},{"text":"Section 8 is needed to determine the full consequence-class vocabulary, modifier set, and compatibility/migration consequences.","type":"evidence_gap","about":"Section 8","quote":"to determine the full consequence-class vocabulary, modifier set, and compatibility/migration consequences"},{"text":"Section 11 is absent if a more precise migration assessment is required.","type":"evidence_gap","about":"Section 11","quote":"Section 11, to determine the required shape, retention, and linkage rules"},{"text":"Section 11 is needed to determine the required shape, retention, and linkage rules for OutboundInteractionLog.","type":"evidence_gap","about":"OutboundInteractionLog","quote":"to determine the required shape, retention, and linkage rules for `OutboundInteractionLog`"},{"text":"Definitions of GraphRelation, the Slice A/B/C predicate lists, and produced_by, executed_by, and spawned record shapes are absent.","type":"evidence_gap","about":"GraphRelation and predicate/record-shape definitions","quote":"The definitions of `GraphRelation`, the Slice A/B/C predicate lists"},{"text":"Those definitions are needed to assess whether future multi-hop traversal can be added without changing stored records.","type":"evidence_gap","about":"future multi-hop traversal","quote":"to assess whether future multi-hop traversal can be added without changing stored records"},{"text":"Applicable definitions of C3, P1, P4, and P7 are absent.","type":"evidence_gap","about":"C3, P1, P4, and P7","quote":"The applicable definitions of C3, P1, P4, and P7"},{"text":"Definitions of C3, P1, P4, and P7 are needed to determine the precise scope of independence, authority, disclosure, and root-authority claims.","type":"evidence_gap","about":"independence, authority, disclosure, and root-authority claims","quote":"to determine the precise scope of independence, authority, disclosure, and root-authority claims"},{"text":"Any retention, versioning, supersession, and migration rules for existing records are absent.","type":"evidence_gap","about":"existing-record retention, versioning, supersession, and migration rules","quote":"Any retention, versioning, supersession, and migration rules for existing records"},{"text":"Retention, versioning, supersession, and migration rules are needed to determine whether changed validation should preserve historical outcomes under original policy versions or rewrite/reclassify them.","type":"evidence_gap","about":"changed validation treatment of historical outcomes","quote":"to determine whether changed validation should preserve historical outcomes under their original policy versions or rewrite/reclassify them"},{"text":"The existing warrant/admission record schemas are absent.","type":"evidence_gap","about":"existing warrant/admission record schemas","quote":"The existing warrant/admission record schemas"},{"text":"The existing warrant/admission record schemas are needed to resolve the expressly open choice between extending them and creating a new DecisionRecord family.","type":"evidence_gap","about":"warrant/admission record-family choice","quote":"needed to resolve the expressly open choice between extending them and creating a new `DecisionRecord` family"}]}
```

## Claude Response

- model: `claude-sonnet-5`

```
{"claims":[
{"text":"Neither proposed instrument has governance force.","type":"finding","about":"the two proposed instruments discussed in the slice","quote":"Neither proposed instrument has governance force."},
{"text":"The table's decisions are prospective design commitments, not admitted requirements.","type":"limitation","about":"status of the decisions listed in the answer","quote":"The following are therefore prospective design commitments, not admitted requirements."},

{"text":"RoleDefinition uses the three axes and carries constraint fields, especially required_verified_properties, required_independence, requires_external_verification, and may_assign_epistemic_status.","type":"finding","about":"RoleDefinition schema","quote":"`RoleDefinition` uses the three axes and carries constraint fields, especially `required_verified_properties`"},
{"text":"EvidenceAdmissionDecision must enumerate verified properties against the receiving role's required_verified_properties.","type":"finding","about":"EvidenceAdmissionDecision constraint from RoleDefinition","quote":"`EvidenceAdmissionDecision` must enumerate verified properties against the receiving role’s `required_verified_properties`"},
{"text":"EvidenceAdmissionDecision cannot treat an uncovered property as established.","type":"finding","about":"EvidenceAdmissionDecision constraint from RoleDefinition","quote":"it cannot treat an uncovered property as established"},
{"text":"Role definitions determine what later admission must prove and which outputs can carry epistemic status.","type":"finding","about":"effect of RoleDefinition on later admission","quote":"Role definitions therefore determine what later admission must prove and which outputs can carry epistemic status."},
{"text":"Changing axes, values, or the meaning of a required property in RoleDefinition would require updating role records and re-evaluating admission decisions that relied on those roles.","type":"finding","about":"RoleDefinition reversal category - migration","quote":"Changing axes, values, or the meaning of a required property would require updating role records and re-evaluating admission decisions that relied on those roles."},
{"text":"Adding a validator for an unchanged RoleDefinition schema is validation-code only.","type":"finding","about":"RoleDefinition reversal category - validation-code only","quote":"Adding a validator for an unchanged role schema is **validation-code only**."},

{"text":"There is a decision distinguishing task, epistemic, action, and accountable authority.","type":"finding","about":"task/epistemic/action/accountable authority distinction","quote":"The distinction between task, epistemic, action, and accountable authority"},
{"text":"Admission cannot infer acceptance authority from artifact production.","type":"finding","about":"authority distinction constraint on admission","quote":"Admission cannot infer acceptance authority from artifact production"},
{"text":"Later lineage work must retain the distinction between an executor, an approver, and an accountable principal.","type":"finding","about":"authority distinction constraint on lineage work","quote":"later lineage work must retain the distinction between an executor, an approver, and an accountable principal"},
{"text":"This distinction requires existing-record migration if identities or fields have been collapsed in stored records.","type":"finding","about":"authority distinction reversal category - migration","quote":"**Existing-record migration** if identities or fields have been collapsed in stored records."},
{"text":"A new validator enforcing an already-recorded separation of authority types is validation-code only.","type":"finding","about":"authority distinction reversal category - validation-code only","quote":"A new validator enforcing an already-recorded separation is **validation-code only**"},
{"text":"Historical records may need reclassification if their prior status depended on the collapse of these authority distinctions.","type":"finding","about":"authority distinction reversal category - possible reclassification","quote":"historical records may need reclassification if their prior status depended on the collapse"},

{"text":"DecisionRecord inherits fields including distinct recommended_status and effective_status, issuer invocation, accountable approver, evidence, policy authority, activation state, and supersession.","type":"finding","about":"DecisionRecord schema","quote":"`DecisionRecord` inherited fields, including distinct `recommended_status` and `effective_status`, issuer invocation, accountable approver, evidence, policy authority, activation state, and supersession"},
{"text":"Later admission requires a record that distinguishes a recommendation from accountable final status and can identify the evidence, authority, and supersession state on which admission relied.","type":"finding","about":"DecisionRecord constraint on later admission","quote":"Later admission requires a record that distinguishes a recommendation from accountable final status and can identify the evidence, authority, and supersession state on which admission relied."},
{"text":"Adding inherited fields or splitting a previously conflated status in DecisionRecord changes persisted decision meaning and requires backfill or explicit unknown values for old records.","type":"finding","about":"DecisionRecord reversal category - migration","quote":"Adding inherited fields or splitting a previously conflated status changes persisted decision meaning and requires backfill or explicit unknown values for old records."},

{"text":"EvidenceAdmissionDecision has admitted artifact or claim, epistemic status, lineage and independence findings, provenance limitations, and verified-property enumeration.","type":"finding","about":"EvidenceAdmissionDecision schema","quote":"`EvidenceAdmissionDecision` has admitted artifact or claim, epistemic status, lineage and independence findings, provenance limitations, and verified-property enumeration"},
{"text":"EvidenceAdmissionDecision is the direct future admission schema.","type":"finding","about":"EvidenceAdmissionDecision role","quote":"This is the direct future admission schema."},
{"text":"EvidenceAdmissionDecision fixes the evidence needed to explain why an artifact or claim entered governed evidence.","type":"finding","about":"EvidenceAdmissionDecision function","quote":"It fixes the evidence needed to explain why an artifact or claim entered governed evidence"},
{"text":"EvidenceAdmissionDecision prevents unsupported property assertions.","type":"finding","about":"EvidenceAdmissionDecision function","quote":"prevents unsupported property assertions"},
{"text":"Records admitted without EvidenceAdmissionDecision's fields cannot simply be treated as if they contained lineage, independence, or verification findings.","type":"finding","about":"EvidenceAdmissionDecision reversal category - migration and re-adjudication","quote":"Records admitted without these fields cannot simply be treated as if they contained lineage, independence, or verification findings."},
{"text":"Validators alone can enforce the EvidenceAdmissionDecision rule only for new decisions.","type":"finding","about":"EvidenceAdmissionDecision reversal category limitation","quote":"Validators alone can enforce the rule only for new decisions."},

{"text":"There is an open choice whether existing warrant/admission records are extended or a new record family is created, and whether kinds are subtypes or one family.","type":"finding","about":"open record-family choice","quote":"The open choice whether existing warrant/admission records are extended or a new record family is created, and whether kinds are subtypes or one family"},
{"text":"This unresolved record-family choice controls record identity, references, query patterns, supersession, and how authorization and admission records relate.","type":"finding","about":"record-family choice effects","quote":"This unresolved choice controls record identity, references, query patterns, supersession, and how authorization and admission records relate."},
{"text":"This decision is an expensive existing-record migration if records are created before the record-family choice is resolved.
```
